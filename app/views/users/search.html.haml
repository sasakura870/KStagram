%h1 User search
= form_with url: search_users_path, method: :get, local: true do |f|
  = f.text_field :name
  = f.submit :search
%table
  %thead
    %tr
      %th Name
  %tbody
    - @users.each do |user|
      %tr
        %td= user.name
